[project]
name = "personal-finances-system"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "argon2-cffi>=23.1.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]